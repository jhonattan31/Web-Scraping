# Extrator de Carta de Serviço
Versão do python utilizada:
- `$ Python 3.8.0`

Instalar Scrapy
- `$ pip install scrapy`

Para executar o extrator de ontologias execute o seguinte comando: 
- `$ scrapy crawl cartaservico -o nomedoarquivo.csv`
